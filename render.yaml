services:
  - type: web
    name: kerala-lottery-final-v7
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node src/index.js
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: WEBHOOK_SECRET
        generateValue: true
      - key: MAX_TICKETS_PER_PHOTO
        value: "10"
      - key: ADMIN_CHAT_ID
        sync: false
